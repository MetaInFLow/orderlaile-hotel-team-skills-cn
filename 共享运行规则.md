# 共享运行规则

所有岗位技能必须遵守本文件。

## 数据源顺序

1. 酒店经营数据：调用订单来了 CLI（工具标识：`mcp__so_agents.so_cli`）。
2. 协作文档、表格、任务、日历、消息：调用飞书 CLI 或钉钉 CLI。
3. 外部报表：只能使用用户上传的文件或用户粘贴的内容。
4. 没有数据：直接说明不知道，不得编造。

## 查询规则

- PMS 语境中的日期必须展开为 `yyyy-MM-dd`。
- “明天”“下周”“月底”等相对日期先换算成明确日期，再提交查询。
- “周末”按周五、周六处理；“平日/工作日”按周日至周四处理，并向用户列出具体日期。
- 用户说“节假日、假期、放假、连休、黄金周”或法定节假日名称时，日期范围必须包含官方放假首日前一天。
- 不确定官方放假安排时先确认，不能执行改价、改库存等写操作。
- 默认使用当前登录凭证所属门店；跨门店必须在问题中写明门店名称。

## 写操作规则

- 改价、改库存、价格码、订单、客户、会员、员工、权限、班次、餐饮、门店设置和营销活动等均属于写操作。
- 写操作前必须先查询现状、唯一定位目标并输出变更预览。
- 预览必须包含门店、日期、房型 / 房间、价格码、旧值、新值及影响。
- 只有用户明确确认后才能执行。
- 跨门店操作必须逐门店展示预览和结果。
- 执行后必须重新查询校验；异步任务要说明“未必立即生效”。
- 失败、超时、部分失败后停止，不自动重试，只能查询最终状态并汇报。
- 订单导入失败后不能修改文件重试，只有用户重新上传才可再次导入。
- ID、房型、价格码、员工、角色和权限标识必须来自本轮查询结果。

## 协作规则

- 飞书 CLI 和钉钉 CLI 仅承载协作内容，不替代 PMS。
- 日报、周报、巡店报告、风险台账和任务清单可写入文档或多维表。
- 经营数字必须标注统计口径、日期范围和数据来源。
- 报告引用外部数据时标记“用户提供文件”，不能装作订单来了实时数据。

## 输出规则

先给结论，再给关键数字、风险、建议和待确认项。  
禁止声称已经发送邮件、同步 OTA、部署网站、抓取官网或更新外部系统，除非当前环境确实有对应 CLI 并返回成功结果。
